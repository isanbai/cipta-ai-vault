<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme for the main kiosk activity.  A no‑action bar
         theme ensures no UI chrome appears. -->
    <style name="Theme.AppCompat.Light.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Material theme used by the settings activity. -->
    <style name="Theme.MaterialComponents.Light.DarkActionBar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@android:color/holo_blue_dark</item>
        <item name="colorPrimaryVariant">@android:color/holo_blue_light</item>
        <item name="colorOnPrimary">@android:color/white</item>
    </style>
</resources>